name: Apply global scene editorial brief

on:
  push:
    branches: [dana-global-scene-brief]

permissions:
  contents: write

jobs:
  integrate:
    if: ${{ !contains(github.event.head_commit.message, 'Integrate global scene editorial brief') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dana-global-scene-brief
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply integration patch
        run: python scripts/apply-global-scene-brief.py
      - name: Verify tests
        run: npm test
      - name: Verify production build
        run: npm run build
      - name: Commit verified integration
        run: |
          git config user.name "DANA AI maintenance"
          git config user.email "actions@users.noreply.github.com"
          git add app/api/generate-voiceover/route.ts
          if git diff --cached --quiet; then
            echo "No integration changes to commit"
          else
            git commit -m "Integrate global scene editorial brief"
            git push origin HEAD:dana-global-scene-brief
          fi
